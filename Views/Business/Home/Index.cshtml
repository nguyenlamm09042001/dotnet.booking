@model booking.ViewModels.BusinessDashboardVm

@{
    ViewData["Title"] = "Dashboard doanh nghiệp";
    var fullName = User.FindFirst("FullName")?.Value;
    var displayName = string.IsNullOrWhiteSpace(fullName) ? (User.Identity?.Name ?? "bạn") : fullName;

    var now = DateTime.Now;

    // ===== Query cho filter recent bookings (dashboard)
    var req = ViewContext.HttpContext.Request;
    var q = (req.Query["q"].ToString() ?? "").Trim();
    var statusFilter = (req.Query["status"].ToString() ?? "").Trim();

    string Norm(string? s) => (s ?? "").Trim().ToLowerInvariant();

    string StatusVi(string s) => Norm(s) switch
    {
        "pending" => "Chờ xác nhận",
        "confirmed" => "Đã xác nhận",
        "completed" => "Hoàn thành",
        "canceled" => "Đã hủy",
        _ => s
    };

    string BadgeClass(string s) => Norm(s) switch
    {
        "completed" => "bg-emerald-100 text-emerald-800 ring-1 ring-emerald-200",
        "canceled"  => "bg-rose-100 text-rose-800 ring-1 ring-rose-200",
        "confirmed" => "bg-sky-100 text-sky-800 ring-1 ring-sky-200",
        "pending"   => "bg-amber-100 text-amber-800 ring-1 ring-amber-200",
        _           => "bg-slate-100 text-slate-800 ring-1 ring-slate-200"
    };

    string Stars(int rating)
    {
        rating = Math.Clamp(rating, 0, 5);
        return new string('★', rating) + new string('☆', 5 - rating);
    }

    
}

<div class="min-h-screen bg-slate-50">
    <div class="mx-auto max-w-7xl px-4 py-8 sm:py-10">

        <!-- Top bar -->
        <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
            <div>
                <div class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-semibold text-slate-600 shadow-sm">
                    <span class="h-2 w-2 rounded-full bg-emerald-500"></span>
                    Business Portal
                </div>

                <h1 class="mt-3 text-2xl font-extrabold tracking-tight text-slate-900 sm:text-3xl">
                    Dashboard doanh nghiệp
                </h1>

                <p class="mt-2 text-sm text-slate-600">
                    Xin chào <span class="font-semibold text-slate-900">@displayName</span> •
                    <span class="text-slate-500">Cập nhật lúc</span>
                    <span class="font-semibold text-slate-700">@now.ToString("HH:mm")</span>
                </p>
            </div>

            <div class="flex flex-col gap-2 sm:flex-row sm:items-center">
                <a asp-controller="BusinessServices" asp-action="Index"
                   class="inline-flex items-center justify-center rounded-2xl bg-indigo-600 px-4 py-3 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700">
                    Quản lý dịch vụ →
                </a>

                <a asp-controller="BusinessBookings" asp-action="Index"
                   class="inline-flex items-center justify-center rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-50">
                    Quản lý lịch hẹn
                </a>

               <a asp-controller="Notifications"
                       asp-action="Index"
                       class="inline-flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">
                        🔔 Thông báo
                    </a>
                <!-- Logout -->
                <form asp-controller="Account" asp-action="Logout" method="post" class="inline-block">
                    @Html.AntiForgeryToken()
                    <button type="submit"
                            class="inline-flex h-11 items-center justify-center rounded-2xl bg-slate-900 px-4 text-sm font-semibold text-white hover:bg-slate-800
                                   focus:outline-none focus:ring-2 focus:ring-slate-400 focus:ring-offset-2">
                        Đăng xuất
                    </button>
                </form>
            </div>
        </div>

        <!-- Alerts / Status -->
        <div class="mt-6 grid grid-cols-1 gap-4 lg:grid-cols-3">
            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="flex items-start justify-between gap-3">
                    <div>
                        <div class="text-sm font-semibold text-slate-900">Tình trạng hệ thống</div>
                        <div class="mt-1 text-sm text-slate-600">
                            Mọi thứ đang hoạt động bình thường.
                        </div>
                    </div>
                    <span class="inline-flex items-center rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">
                        OK
                    </span>
                </div>
            </div>

           
<div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
    <div class="flex items-start justify-between gap-3">
        <div>
            <div class="text-sm font-semibold text-slate-900">Việc cần làm</div>
            <div class="mt-1 text-sm text-slate-600">
                @Model.PendingBookings lịch chờ xác nhận • @Model.PendingSwapRequests yêu cầu đổi ca • @Model.PendingReviews phản hồi cần xử lý
            </div>
        </div>

        @{
            var todoTotal = Model.PendingBookings + Model.PendingSwapRequests + Model.PendingReviews;
            var badgeClass = todoTotal > 0 ? "bg-amber-50 text-amber-700" : "bg-emerald-50 text-emerald-700";
            var badgeText = todoTotal > 0 ? "To-do" : "OK";
        }

        <span class="inline-flex items-center rounded-full px-3 py-1 text-xs font-bold @badgeClass">
            @badgeText
        </span>
    </div>
</div>




            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="flex items-start justify-between gap-3">
                    <div>
                        <div class="text-sm font-semibold text-slate-900">Chất lượng dịch vụ</div>
                        <div class="mt-1 text-sm text-slate-600">
                            Điểm trung bình:
                            <span class="font-semibold text-slate-900">@Model.AvgRating.ToString("0.0")</span>
                            • Tỷ lệ hủy:
                            <span class="font-semibold text-slate-900">@Model.CancelRate%</span>
                        </div>
                    </div>
                    <span class="inline-flex items-center rounded-full bg-sky-50 px-3 py-1 text-xs font-bold text-sky-700">
                        Insights
                    </span>
                </div>
            </div>
        </div>

        <!-- KPI Cards -->
        <div class="mt-6 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-6">
            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2">
                <div class="text-xs font-semibold uppercase tracking-wide text-slate-500">Doanh thu tháng</div>
                <div class="mt-2 text-3xl font-extrabold tracking-tight text-slate-900">
                    @Model.RevenueMonth.ToString("N0")đ
                </div>
                <div class="mt-2 text-sm text-slate-600">
                    So với tháng trước: <span class="font-semibold text-emerald-700">+0%</span>
                </div>
                <div class="mt-4 h-2 w-full overflow-hidden rounded-full bg-slate-100">
                    <div class="h-full w-[35%] rounded-full bg-indigo-600"></div>
                </div>
                <div class="mt-2 text-xs text-slate-500">Tiến độ mục tiêu tháng (demo)</div>
            </div>

            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="text-xs font-semibold uppercase tracking-wide text-slate-500">Dịch vụ</div>
                <div class="mt-2 text-3xl font-extrabold tracking-tight text-slate-900">@Model.ServicesActive</div>
                <div class="mt-2 text-sm text-slate-600">Đang hoạt động</div>
            </div>

            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="text-xs font-semibold uppercase tracking-wide text-slate-500">Lịch hôm nay</div>
                <div class="mt-2 text-3xl font-extrabold tracking-tight text-slate-900">@Model.BookingsToday</div>
                <div class="mt-2 text-sm text-slate-600">Hôm nay</div>
            </div>

            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="text-xs font-semibold uppercase tracking-wide text-slate-500">Review mới</div>
                <div class="mt-2 text-3xl font-extrabold tracking-tight text-slate-900">@Model.NewReviews7d</div>
                <div class="mt-2 text-sm text-slate-600">7 ngày gần đây</div>
            </div>

            <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
                <div class="text-xs font-semibold uppercase tracking-wide text-slate-500">Điểm TB</div>
                <div class="mt-2 text-3xl font-extrabold tracking-tight text-slate-900">@Model.AvgRating.ToString("0.0")</div>
                <div class="mt-2 text-sm text-slate-600">Trên 5.0</div>
            </div>
        </div>

        <!-- Main content grid -->
        <div class="mt-6 grid grid-cols-1 gap-6 lg:grid-cols-12">

            <!-- Left: Recent bookings table -->
            <div class="lg:col-span-8">
                <div class="rounded-3xl border border-slate-200 bg-white shadow-sm">
                    <div class="flex flex-col gap-3 px-6 py-5 sm:flex-row sm:items-center sm:justify-between">
                        <div>
                            <div class="text-lg font-extrabold text-slate-900">Lịch hẹn gần đây</div>
                            <div class="mt-1 text-sm text-slate-600">Dữ liệu mới nhất từ hệ thống</div>
                        </div>

                        <!-- Filters (AUTO SUBMIT - không cần nút) -->
                        <form id="recentBookingsFilter"
                              method="get"
                              class="flex flex-col gap-2 sm:flex-row sm:items-center">
                            <select name="status"
                                    class="h-11 rounded-2xl border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700 shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-300">
                                <option value="" selected="@(string.IsNullOrWhiteSpace(statusFilter))">Tất cả trạng thái</option>
                                <option value="pending" selected="@(statusFilter == "pending")">Chờ xác nhận</option>
                                <option value="confirmed" selected="@(statusFilter == "confirmed")">Đã xác nhận</option>
                                <option value="completed" selected="@(statusFilter == "completed")">Hoàn thành</option>
                                <option value="canceled" selected="@(statusFilter == "canceled")">Đã hủy</option>
                            </select>

                            <input name="q" value="@q"
                                   class="h-11 w-full rounded-2xl border border-slate-200 bg-white px-3 text-sm text-slate-700 shadow-sm
                                          focus:outline-none focus:ring-2 focus:ring-indigo-300 sm:w-64"
                                   placeholder="Tìm theo tên / SĐT / dịch vụ..." />
                        </form>

                        
                    </div>

                    <div class="h-px bg-slate-200"></div>

                    <div class="overflow-x-auto">
                        <table class="min-w-full text-left text-sm">
                            <thead class="bg-slate-50">
                                <tr class="text-xs font-bold uppercase tracking-wide text-slate-500">
                                    <th class="px-6 py-4">Khách</th>
                                    <th class="px-6 py-4">Dịch vụ</th>
                                    <th class="px-6 py-4">Thời gian</th>
                                    <th class="px-6 py-4">Trạng thái</th>
                                    <th class="px-6 py-4 text-right">Hành động</th>
                                </tr>
                            </thead>

                            <tbody class="divide-y divide-slate-200 bg-white">
                                @if (Model.RecentBookings == null || !Model.RecentBookings.Any())
                                {
                                    <tr>
                                        <td class="px-6 py-6 text-slate-500" colspan="5">
                                            Chưa có lịch hẹn nào.
                                        </td>
                                    </tr>
                                }
                                else
                                {
                                    @foreach (var b in Model.RecentBookings)
                                    {
                                        <tr class="hover:bg-slate-50">
                                            <td class="px-6 py-4">
                                                <div class="font-semibold text-slate-900">@b.CustomerName</div>
                                                <div class="text-xs text-slate-500">@b.Phone</div>
                                            </td>

                                            <td class="px-6 py-4 text-slate-700">@b.ServiceName</td>

                                            <td class="px-6 py-4 text-slate-700">
                                                @b.Date.ToString("dd/MM") • @b.Time.ToString("HH:mm")
                                            </td>

                                            <td class="px-6 py-4">
                                                <span class="inline-flex items-center rounded-full px-3 py-1 text-xs font-bold @BadgeClass(b.Status)">
                                                    @StatusVi(b.Status)
                                                </span>
                                            </td>

                                            <td class="px-6 py-4">
                                                <div class="flex justify-end gap-2">
                                                    <a asp-controller="BusinessBookings" asp-action="Details" asp-route-id="@b.Id"
                                                       class="inline-flex h-10 min-w-[92px] items-center justify-center rounded-xl border border-slate-200 bg-white px-3 text-xs font-bold text-slate-700 shadow-sm hover:bg-slate-50">
                                                        Xem
                                                    </a>

                                                    @if (b.Status == "pending")
                                                    {
                                                        <form asp-controller="BusinessBookings" asp-action="Approve" method="post">
                                                            @Html.AntiForgeryToken()
                                                            <input type="hidden" name="id" value="@b.Id" />
                                                            <button type="submit"
                                                                    class="inline-flex h-10 min-w-[92px] items-center justify-center rounded-xl bg-emerald-600 px-3 text-xs font-bold text-white shadow-sm hover:bg-emerald-700">
                                                                Duyệt
                                                            </button>
                                                        </form>
                                                    }
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>

                    <div class="flex flex-col gap-3 px-6 py-5 sm:flex-row sm:items-center sm:justify-between">
                        <div class="text-sm text-slate-600">
                            Hiển thị <span class="font-semibold text-slate-900">@(Model.RecentBookings?.Count ?? 0)</span> mục
                        </div>

                     <a asp-controller="BusinessBookings" asp-action="Index"
   asp-route-q="@q"
   asp-route-status="@statusFilter"
   class="inline-flex items-center justify-center rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-50">
    Xem tất cả lịch hẹn →
</a>

                    </div>
                </div>
            </div>

            <!-- Right: Quick actions + Recent reviews -->
            <div class="lg:col-span-4 space-y-6">

                <!-- Quick actions -->
                <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
                    <div class="text-lg font-extrabold text-slate-900">Hành động nhanh</div>

<!-- Marketing suggestion -->
<div class="rounded-3xl border border-amber-200 bg-amber-50/60 p-6 shadow-sm">
    <div class="flex items-start justify-between gap-3">
        <div>
            <div class="text-lg font-extrabold text-slate-900">Tăng đơn nhanh bằng Ads</div>
            <div class="mt-1 text-sm text-slate-700">
                Gói ưu tiên hiển thị <span class="font-bold">15.000đ / ngày</span>.
                Quảng cáo sẽ lên mục <span class="font-semibold">“Được tài trợ”</span> ở trang Khám phá sau khi admin duyệt.
            </div>
        </div>

        <span class="inline-flex items-center rounded-full bg-white px-3 py-1 text-xs font-bold text-amber-800 ring-1 ring-amber-200">
            NEW
        </span>
    </div>

    <div class="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-2">
        <div class="rounded-2xl border border-amber-200 bg-white p-4">
            <div class="text-xs font-semibold uppercase tracking-wide text-amber-700">Gợi ý</div>
            <div class="mt-1 text-sm text-slate-700">
                Phù hợp khi hôm nay ít lịch, hoặc muốn đẩy dịch vụ mới.
            </div>
        </div>

        <div class="rounded-2xl border border-amber-200 bg-white p-4">
            <div class="text-xs font-semibold uppercase tracking-wide text-amber-700">Chi phí</div>
            <div class="mt-1 text-sm text-slate-700">
                Ví dụ: 2 ngày = <span class="font-bold">30.000đ</span>
            </div>
        </div>
    </div>

    <div class="mt-4 grid grid-cols-1 gap-2 sm:grid-cols-2">
        <!-- CTA chính: đổi text thành "Quảng cáo ngay" -->
      <a asp-controller="BusinessMarketing" asp-action="Create"
   class="inline-flex h-12 w-full items-center justify-center rounded-2xl
          border border-amber-200 bg-white px-4
          text-sm font-semibold text-slate-900 hover:bg-amber-50">
    Quảng cáo ngay →
</a>


        <!-- CTA phụ -->
        <button type="button"
                class="inline-flex h-12 w-full items-center justify-center rounded-2xl border border-amber-200 bg-white px-4
                       text-sm font-semibold text-amber-900 hover:bg-amber-50"
                onclick="Swal.fire({icon:'info', title:'Ads 15.000đ/ngày', text:'Bấm Quảng cáo ngay để chọn ngày chạy và số ngày. Admin duyệt xong là doanh nghiệp lên mục Được tài trợ ở trang Khám phá.'})">
            Xem cách hoạt động
        </button>
    </div>
</div>



                    <p class="mt-1 text-sm text-slate-600">Các thao tác thường dùng cho chủ doanh nghiệp.</p>

                    <div class="mt-5 space-y-3">
                        <a asp-controller="BusinessServices" asp-action="Create"
                           class="flex items-center justify-between rounded-2xl border border-slate-200 bg-white px-4 py-4 text-sm font-semibold text-slate-800 shadow-sm hover:bg-slate-50">
                            <span>+ Tạo dịch vụ mới</span>
                            <span class="text-slate-400">→</span>
                        </a>

                        <a asp-controller="BusinessProfile" asp-action="Index"
                           class="flex items-center justify-between rounded-2xl border border-slate-200 bg-white px-4 py-4 text-sm font-semibold text-slate-800 shadow-sm hover:bg-slate-50">
                            <span>Cập nhật hồ sơ doanh nghiệp</span>
                            <span class="text-slate-400">→</span>
                        </a>

                        <a asp-controller="BusinessStaff" asp-action="Index"
                           class="flex items-center justify-between rounded-2xl border border-slate-200 bg-white px-4 py-4 text-sm font-semibold text-slate-800 shadow-sm hover:bg-slate-50">
                            <span>Quản lý nhân viên</span>
                            <span class="text-slate-400">→</span>
                        </a>
                    </div>

                    <div class="mt-5 rounded-2xl bg-indigo-50 p-4">
                        <div class="text-sm font-bold text-indigo-900">Tip</div>
                        <div class="mt-1 text-sm text-indigo-900/80">
                            Tối ưu demo: gắn KPI thật + danh sách lịch thật + review thật là “ăn điểm” nhất.
                        </div>
                    </div>
                </div>

                <!-- Recent reviews -->
                <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
                    <div class="flex items-center justify-between">
                        <div>
                            <div class="text-lg font-extrabold text-slate-900">Đánh giá gần đây</div>
                            <div class="mt-1 text-sm text-slate-600">Hiển thị các review mới nhất.</div>
                        </div>
                        <span class="inline-flex items-center rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-700">
                            Live
                        </span>
                    </div>

                    <div class="mt-5 space-y-4">
                        @if (Model.RecentReviews == null || !Model.RecentReviews.Any())
                        {
                            <div class="text-sm text-slate-500">
                                Chưa có đánh giá nào.
                            </div>
                        }
                        else
                        {
                            @foreach (var r in Model.RecentReviews)
                            {
                                <div class="rounded-2xl border border-slate-200 bg-white p-4">
                                    <div class="flex items-start justify-between">
                                        <div class="font-semibold text-slate-900">Khách: @r.CustomerName</div>
                                        <div class="text-xs font-bold text-amber-600">@Stars(r.Rating)</div>
                                    </div>
                                    <div class="mt-1 text-sm text-slate-600">
                                        @(string.IsNullOrWhiteSpace(r.Comment) ? "—" : r.Comment)
                                    </div>
                                    <div class="mt-2 text-xs text-slate-500">@r.CreatedAt.ToString("dd/MM HH:mm")</div>
                                </div>
                            }
                        }
                    </div>

                    <div class="mt-5">
                        <a asp-controller="BusinessReviews" asp-action="Index"
                           class="inline-flex w-full items-center justify-center rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-50">
                            Xem tất cả đánh giá →
                        </a>
                    </div>
                </div>

            </div>
        </div>

        <!-- Footer note -->
        <div class="mt-8 text-sm text-slate-500">
            Gợi ý: Dashboard “đủ lực” khi có KPI, bảng lịch hẹn, review, quick actions, và phần pending approvals (đang
            chờ duyệt).
        </div>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            const form = document.getElementById("recentBookingsFilter");
            if (!form) return;

            const status = form.querySelector("select[name='status']");
            const q = form.querySelector("input[name='q']");

            // đổi dropdown là submit liền
            status?.addEventListener("change", () => form.submit());

            // gõ là tự submit (debounce)
            let t;
            q?.addEventListener("input", () => {
                clearTimeout(t);
                t = setTimeout(() => form.submit(), 350);
            });
        })();
    </script>
}
